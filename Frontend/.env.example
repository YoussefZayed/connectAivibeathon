# The base URL for your production backend. This is REQUIRED for production builds.
API_URL=https://api.your-production-domain.com

# (Optional) The base URL for your development backend.
# Overrides the default platform-specific URLs (localhost for web, 10.0.2.2 for native).
# DEV_API_URL=http://your-dev-api.com